

// var toggle = false;
    // chrome.tabs.executeScript(null, {file: "content.js"});

// chrome.browserAction.onClicked.addListener(function(tab) {
  // toggle = !toggle;
  
  // chrome.tabs.sendMessage(tab.id, {state:toggle}, function(response) {});

  
  // if(toggle){
    // chrome.browserAction.setIcon({path: "img/on.png", tabId:tab.id});
  // }
  // else{
    // chrome.browserAction.setIcon({path: "img/off.png", tabId:tab.id});
  // }

// });

